name: Plugin Dependency Update

# DISABLED: scripts/check-plugin-updates.js does not exist yet.
# Uncomment this workflow once the script is created.
on:
  schedule:
    - cron: '0 0 * * 1' # Every Monday at midnight UTC
  workflow_dispatch:

jobs:
  check-plugin-updates:
    name: Check for plugin updates
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Check plugin updates
        run: |
          echo "::error::Plugin update check is not implemented yet."
          echo "::error::Create scripts/check-plugin-updates.js to enable this workflow."
          exit 1
